<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright © Cliqz 2018-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bond_dashboard_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:orientation="vertical">

    <TextView
        android:id="@+id/dashboard_state_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textAlignment="center"
        android:textColor="@color/bond_general_color_blue"
        android:textSize="20sp"
        tools:text="@string/bond_dashboard_ultimate_protection" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/dash_board_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/dashboard_state_text"
        android:padding="16dp"/>

    <View
        android:id="@+id/dashboard_disable_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/dashboard_state_text"
        android:background="@color/dark_transparent_overlay"
        android:visibility="gone" />

</RelativeLayout>
